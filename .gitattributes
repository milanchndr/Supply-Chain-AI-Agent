# Set default behavior for all files to normalize line endings
* text=auto

# Explicitly set text files to always use LF in the repo
*.py text eol=lf
*.md text eol=lf
*.json text eol=lf
*.txt text eol=lf
*.csv text eol=lf # You can add this for CSVs too
# Add other text file extensions as needed

# Mark binary files to prevent line ending conversion (if any)
# *.png binary
# *.jpg binary